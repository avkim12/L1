// вывод: 1 1

func update(p *int) {
	b := 2 
	p = &b // копии p присваивается новый адрес
}

func main() {
	var (
		a = 1
		p = &a // p - указатель на a
	)
	fmt.Println(*p) // выводится значение по адресу
	update(p) // p отправляется по значению
	fmt.Println(*p) // выводится старое значение
}